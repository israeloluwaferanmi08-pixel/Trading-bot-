pandas>=2.0
numpy>=1.24
requests>=2.28
python-dotenv>=1.0
psutil>=5.9   # used for the /status and heartbeat memory/CPU readout

# Optional, only needed for the features you actually use:
# matplotlib>=3.7        # for run_backtest.py --plot
# MetaTrader5>=5.0        # live data on Windows via your broker's MT5 terminal
# ccxt>=4.0                # fallback BTCUSD data source via a crypto exchange
