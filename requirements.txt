pandas>=2.0
numpy>=1.24
requests>=2.28
python-dotenv>=1.0
psutil>=5.9   # used for the /status and heartbeat memory/CPU readout
ccxt>=4.0     # primary BTCUSD data source via a crypto exchange
flask>=3.0    # web admin dashboard
waitress>=3.0 # production WSGI server for the dashboard (falls back to Flask's dev server if missing)

# Optional:
# matplotlib>=3.7        # for run_backtest.py --plot
